#!/usr/bin/env ruby

$LOAD_PATH.push File.expand_path('../lib', __dir__)

require "fnode"

FNode::Server.run!

# server = File.expand_path(File.join('..', 'lib', 'fnode', 'fnode.rb'), __dir__)
# puts server
# system "ruby #{server}"
